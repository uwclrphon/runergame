def wantif(text,ifmun):
    if text == ifmun:
        return True
    else:
        return False
class math:
    def __init__(self):
        import datetime,os,pygame,sys,random
        self.date =  datetime.datetime.now()
        self.gpt = os.getcwd()
        self.version = sys.version
        self.ghiu = random
        self.pyi = pygame
    def rom(self,a,b):
        return self.ghiu.randrange(a,b)
    def dateget(self):
        return self.version